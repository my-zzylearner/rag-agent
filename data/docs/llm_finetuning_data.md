---
topic: llm finetuning data
keywords: 
description: 聚焦大模型指令微调与参数高效优化，涵盖LoRA、SFT、数据量规划与Qwen适配。适用于判断与大模型微调相关的知识路由。
type: knowledge_base
---



---
## 补充知识（来自网络搜索）
> query: Qwen 微调 需要多少数据 LoRA SFT 数据量建议 | 更新时间: 2026-04-19

### Qwen 模型微调：LoRA 与 SFT 数据量指南

- **核心概念与对比**
  - **SFT（监督微调）**：通过高质量指令数据更新模型权重，分为全参微调与参数高效微调。
  - **LoRA（低秩自适应）**：冻结主干网络，仅训练低秩分解矩阵。原理是通过旁路注入可训练参数，大幅降低显存占用与数据依赖，适合垂直领域快速适配。
- **数据量建议**
  - **LoRA 微调**：推荐 **1,000~10,000 条**高质量指令对。单一垂直任务 500~2,000 条即可收敛；多任务/通用能力增强建议 5k+。
  - **全参 SFT**：通常需 **10,000~100,000+ 条**，且需严格去重、清洗与格式对齐。
  - **核心原则**：质量 > 数量。数据需覆盖目标场景，避免逻辑冲突与噪声；推荐统一采用 ShareGPT/Alpaca 格式。
- **工程实践参考**
  - LoRA 显著降低硬件门槛（如 Qwen-14B LoRA 单卡 24GB 可跑）；全参微调 32B 需多卡 A800 配合 DeepSpeed ZeRO-3 防 OOM。
  - 关键超参：`rank (r=8~32)`、`alpha`、`lr (1e-4~5e-4)`、`epochs (2~3)`。

来源：
- https://blog.csdn.net/...
- https://github.com/QwenLM/Qwen2.5/issues/6999
- https://zhuanlan.zhihu.com/...
- https://www.cnblogs.com/...


---
## 补充知识（来自网络搜索）
> query: 高质量 SFT 数据集构建技巧 LLM 微调 清洗 | 更新时间: 2026-04-19

## 核心概念
- **SFT（监督微调）**：在预训练基座模型上，使用结构化指令-回复对进行有监督训练，使模型对齐人类意图、掌握领域知识或遵循特定输出格式。
- **数据驱动**：SFT效果高度依赖数据质量，“数据决定上限”，劣质数据易导致模型幻觉或灾难性遗忘。

## 数据清洗策略
- **基础过滤**：剔除重复样本、乱码、过短/超长文本及非目标语言内容。
- **安全合规**：结合正则规则与分类模型，拦截敏感、偏见、有害及含隐私信息的数据。
- **格式规范**：统一转为标准JSONL，确保`instruction`、`input`、`output`字段完整，严格匹配Prompt模板。

## 构建与合成技巧
- **多源融合**：整合开源指令集、垂直领域文档、真实用户日志与人工精标数据，覆盖长尾场景。
- **模型合成**：利用强基座模型生成CoT推理、角色扮演及多轮对话数据，引入“自一致性”或交叉验证过滤错误输出。
- **难度分层**：按任务复杂度划分基础、进阶与挑战样本，控制分布比例，避免模型过度拟合简单模式。

## 质量评估原则
- **多样性**：覆盖多领域、多句式与交互风格，保障泛化能力。
- **准确性**：答案需逻辑严密、事实可考，关键数据建议专家复核。
- **可控性**：监控微调前后性能指标，动态调整数据配比以平衡新技能学习与旧知识保留。

## 来源 URL
- LLM微调实践 - 李乾坤的博客
- LLM的SFT - 知乎专栏
- 1.3 大模型监督微调（SFT）的数据构造与数据合成 - CSDN博客
- 大模型微调数据准备全指南：清洗、标注与高质量训练集构造实战


---
## 补充知识（来自网络搜索）
> query: SFT 数据集格式 ChatML Alpaca 微调 防止灾难性遗忘 | 更新时间: 2026-04-19

### SFT 微调与数据集格式
- **SFT（监督微调）**：预训练后的核心对齐步骤，利用高质量“指令-回复”对更新模型权重，使其掌握特定领域知识与交互规范。
- **ChatML 格式**：面向多轮对话。使用特殊占位符（如 `<|im_start|>`、`<|im_end|>`）严格划分 system/user/assistant 角色，支持长上下文状态保持，为当前主流开源基座默认格式。
- **Alpaca 格式**：面向单轮任务。采用 JSON 结构，固定包含 `instruction`（指令）、`input`（可选上下文）、`output`（目标回复）三字段，解析简单，适合垂直场景快速适配。

### 防止灾难性遗忘
- **原理**：SFT 阶段全量或大规模参数更新会覆盖预训练学到的通用语言规律，导致模型在基础能力上出现性能断崖式下跌。
- **核心对策**：
  - **数据混合**：按 5%~15% 比例掺入高质量预训练语料，维持知识分布连续性。
  - **参数高效微调**：优先采用 LoRA/QLoRA，冻结主干网络，仅训练低秩适配器，物理隔离原始权重。
  - **学习率约束**：使用极小 LR（1e-5~5e-5）配合线性 Warmup，限制梯度步幅，避免权重剧烈偏移。
  - **经验回放**：训练批次中随机注入历史样本，通过对比损失抑制旧知识衰减。

来源：https://github.com/datawhalechina/diy-llm/blob/main/docs/chapter13/chapter13_第十三章大模型的基本训练流程.md
