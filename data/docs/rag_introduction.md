---
topic: RAG技术原理
keywords: rag, retrieval-augmented generation, chunking, embedding, vector search, hyde, reranking, rag optimization
description: 介绍RAG（检索增强生成）的完整技术原理，包括离线索引与在线检索流程、Chunking分块策略、Embedding模型选型、查询优化（HyDE/多查询/改写）及检索优化（混合检索/Reranking）方向，适用于判断与RAG系统构建、优化相关的知识路由。
type: knowledge_base
---

# RAG（检索增强生成）技术详解

## 什么是 RAG

RAG（Retrieval-Augmented Generation，检索增强生成）是一种将信息检索与大语言模型生成能力结合的技术框架。它解决了大语言模型的核心局限：知识截止日期和幻觉问题。

RAG 的核心思路是：在生成回答之前，先从外部知识库中检索与问题相关的文档片段，然后将这些片段作为上下文（Context）提供给 LLM，引导模型基于真实信息生成回答。

## RAG 的工作流程

### 离线阶段（索引构建）
1. **文档加载**：从各种来源（PDF、网页、数据库）加载原始文档
2. **文本分块（Chunking）**：将长文档切割成固定大小的文本块
3. **向量化（Embedding）**：用 Embedding 模型将文本块转换为高维向量
4. **存储**：将向量和原始文本存入向量数据库

### 在线阶段（检索与生成）
1. **查询向量化**：将用户问题转换为向量
2. **相似度检索**：在向量数据库中找到最相似的 top-k 个文本块
3. **Prompt 构造**：将检索到的文本块拼接到 Prompt 中
4. **LLM 生成**：模型基于检索内容生成最终回答

## Chunking 策略

Chunking 是 RAG 中最关键的工程决策之一，直接影响检索质量。

### 固定大小分块
最简单的方式，按字符数或 token 数切割：
- **chunk_size**：每块大小，通常 256~1024 tokens
- **chunk_overlap**：相邻块的重叠部分，通常为 chunk_size 的 10%~20%

重叠的目的是保留跨块的上下文信息，避免关键信息被切断。

### 语义分块
基于句子边界、段落、标题等语义单位切割，比固定大小更智能，但实现复杂。

### 选择建议
- 问答类任务：chunk_size=512，overlap=64
- 长文档摘要：chunk_size=1024，overlap=128
- 代码文档：按函数/类边界切割

## Embedding 模型选择

| 模型 | 维度 | 特点 |
|------|------|------|
| all-MiniLM-L6-v2 | 384 | 轻量，速度快，适合英文 |
| text-embedding-3-small | 1536 | OpenAI，效果好，需要 API |
| bge-large-zh | 1024 | 中文效果优秀，开源 |
| e5-large-v2 | 1024 | 多语言，效果均衡 |

## RAG 的优化方向

### 查询优化
- **查询改写**：用 LLM 将用户问题改写为更适合检索的形式
- **HyDE（假设文档嵌入）**：先让 LLM 生成一个假设答案，用假设答案做检索
- **多查询**：生成多个不同角度的查询，合并检索结果

### 检索优化
- **混合检索**：结合向量检索（语义）和 BM25（关键词），互补不足
- **重排序（Reranking）**：用 Cross-Encoder 对初步检索结果重新排序
- **父文档检索**：检索小块，返回大块，兼顾精度和上下文

### 生成优化
- **引用标注**：在回答中标注信息来源，提高可信度
- **答案验证**：检查生成答案是否有检索内容支撑

## RAG vs 微调

| 维度 | RAG | 微调 |
|------|-----|------|
| 知识更新 | 实时，更新文档即可 | 需要重新训练 |
| 成本 | 低 | 高 |
| 可解释性 | 高（可追溯来源） | 低 |
| 适用场景 | 知识库问答、文档检索 | 特定风格、领域适配 |

## 常见问题

**Q: RAG 和传统搜索引擎有什么区别？**
传统搜索返回文档链接，RAG 直接生成自然语言答案。RAG 理解语义，传统搜索依赖关键词匹配。

**Q: 向量检索找不到相关内容怎么办？**
可以结合 BM25 关键词检索（混合检索），或优化 Chunking 策略，或对查询进行改写。

**Q: 如何评估 RAG 效果？**
常用指标：Faithfulness（忠实度）、Answer Relevancy（答案相关性）、Context Recall（上下文召回率）。工具：RAGAS 框架。


---
## 补充知识（来自网络搜索）
> query: AI RAG chunk分片算法 文本分块策略 技术原理 | 更新时间: 2026-04-12 | hash: 2a208debed1e82466fbf67d39f68b5d8

# RAG 文本分块（Chunking）策略与技术原理

## 核心概念
将长文本分割为适合向量数据库存储的片段，是 RAG 系统检索精度与生成质量的基础环节。

## 主流分块策略
- **固定长度**：按字符或 Token 数切分，实现简单但易割裂语义。
- **递归分块**：基于段落、标题等自然分隔符递归切割，保留文档结构。
- **语义分块**：利用 NLP 模型识别语义边界，确保上下文逻辑完整。
- **父子分块**：父块用于检索定位，子块提供详细内容，平衡效率与精度。

## 关键技术点
- **重叠（Overlap）**：相邻块保留部分重复内容，防止关键信息在边界丢失。
- **粒度平衡**：块过大引入噪声，过小丢失上下文，需依场景调整。
- **领域适配**：针对代码、法律、医疗等不同文本类型定制分块规则。

## 选型建议
无通用最优解，建议结合具体数据集进行多策略对比测试。

**来源参考：**
1. CSDN：【RAG】11 种 Chunking Strategies 分块策略介绍和选择
2. 知乎：LLM 开发者必备：掌握 21 种分块策略让 RAG 应用性能翻倍
3. 博客园：RAG 分块策略：从原理到实战优化，喂饭级教程不允许你踩坑


---
## 补充知识（来自网络搜索）
> query: RAG chunk splitting algorithm 固定长度递归语义分块 重叠策略详解 | 更新时间: 2026-04-12 | hash: cb671f442797da0e35713dd04a4f6bbe

# RAG 文档分块（Chunking）策略详解

## 1. 核心分块算法
- **固定长度分块**：按字符或 Token 数均匀切割。优点是处理速度快，缺点是易切断语义完整性。
- **递归字符分块**：优先按段落、句子等自然分隔符切分，不足时再按固定长度补全。兼顾了文档结构与灵活性。
- **语义分块**：基于 Embedding 相似度计算，在语义边界处切分。能最大程度保留上下文关联，但计算成本较高。

## 2. 重叠策略（Overlap）
- **原理**：相邻分块间保留部分重复内容，形成滑动窗口。
- **作用**：防止关键信息因切分而丢失，确保检索时上下文连贯，提升召回率。
- **配置**：通常设置为分块大小的 10%-20%，需结合模型上下文窗口调整。

## 3. 策略对比与选型
- **精度 vs 速度**：固定长度最快，语义分块最准。
- **适用场景**：结构化数据推荐递归分块；长文本深度理解推荐语义分块。
- **原则**：在检索精度与处理延迟间取得平衡。

来源：基于搜索结果标题及技术共识整理


---
## 补充知识（来自网络搜索）
> query: ragas评测机制 RAG评估 | 更新时间: 2026-04-12 | hash: a3b4c906cfddfaa61ba7a2a5c210646c

# RAGAS 评估机制

### 定义
RAGAS (Retrieval Augmented Generation Assessment) 是一个专注于 RAG（检索增强生成）系统的自动化评估框架。它利用大语言模型（LLM）作为评判者，旨在替代传统的人工评估，实现对 RAG 流程的无参考、快速迭代评估。

### 核心评估指标
RAGAS 通过以下四个核心维度量化系统性能：

*   **忠实度**：衡量生成的答案是否完全源于检索到的上下文，用于检测模型“幻觉”。若答案中的声明无法在上下文中找到依据，则忠实度低。
*   **答案相关性**：衡量答案是否准确回应了原始问题。评估方式通常让 LLM 根据答案反向生成问题，计算生成问题与原问题的相似度。
*   **上下文精确度**：衡量检索到的上下文中是否包含无关信息。重点评估检索系统区分相关文档与噪声的能力。
*   **上下文召回率**：衡量检索内容是否覆盖了回答问题所需的所有背景信息，评估检索系统的全面性。

### 工作原理
该框架采用 **"LLM-in-the-loop"** 机制。通过精心设计的提示词，让高性能 LLM（如 GPT-4）对检索内容和生成答案进行推理、验证和打分，最终计算出各项指标的数值，帮助开发者快速定位检索或生成模块的问题。

### 来源
1. 【RAG评估】1. 通俗易懂：深度理解RAGAS评估方法的原理与应用
2. RAGAS 框架：快速自动评估RAG 质量，还便于集成 - 飞书文档
3. Ragas评测框架深度解析：彻底改变你的RAG系统评估方式
4. 深度测评 RAG 应用评估框架：指标最全面的 RAGas-腾讯云开发者社区-腾讯云


---
## 补充知识（来自网络搜索）
> query: RAGAS五大指标 Faithfulness Answer Relevance Context Relevance Context Precision Context Recall 详细介绍 | 更新时间: 2026-04-18 | hash: 1cd9d2bc769f989afc5b0fd742a118d6

### RAGAS 评估框架五大核心指标

RAGAS 是用于评估检索增强生成（RAG）系统的综合框架，其五大核心指标涵盖生成与检索质量：

*   **Faithfulness (忠实度)**：衡量生成答案的事实依据，评估答案是否严格由检索到的上下文支持，用于检测幻觉。
*   **Answer Relevance (答案相关性)**：评估生成答案对用户查询的直接针对程度，判断答案是否切题或答非所问。
*   **Context Relevance (上下文相关性)**：衡量检索到的上下文是否包含回答问题所需的关键信息，用于过滤噪声数据。
*   **Context Precision (上下文精确度)**：评估检索结果中相关文档的排序质量，即相关文档是否排在前列。
*   **Context Recall (上下文召回率)**：衡量检索到的上下文是否覆盖了标注答案（Truth）中的所有关键信息，检查信息完整性。

**来源：**
1. 小白学大模型：使用Ragas评价RAG效果 - 墨天轮
2. Metrics | Ragas
3. RAGAS|RAG系统的综合评估框架（含precision详解） - 知乎专栏
4. RAGAS的关键指标与评估维度 - 飞书文档


---
## 补充知识（来自网络搜索）
> query: embedding 模型种类 使用场景 sentence-transformers OpenAI BGE | 更新时间: 2026-04-19

### Embedding 模型与选型

**核心概念**
Embedding 将文本映射为高维向量，向量间的距离反映语义相似度。它是连接非结构化文本与向量数据库的桥梁。

**主流模型对比**
*   **OpenAI Embeddings**：闭源 SOTA 代表（如 `text-embedding-3`）。多语言能力强，中文效果稳健，API 调用简单，适合无需私有化部署的快速开发，但需付费。
*   **BGE (BAAI General Embedding)**：目前最强的开源模型系列。针对中文场景深度优化，训练数据广泛，支持微调，性价比极高，是本地部署的首选。
*   **Sentence-Transformers**：基于 PyTorch 的框架（含 SBERT 等模型）。生态丰富，易于训练和微调，适合学术研究及特定领域的定制化需求。

**主要使用场景**
语义搜索、RAG（检索增强生成）系统的知识库召回、文档聚类、推荐系统及语义去重。

**来源**
- BGE论文解读：如何炼成中文世界的通用Embedding Model - 文章
- 探索Hugging Face上的BGE模型：开源嵌入的最佳选择 - 稀土掘金
- (LLM系列)Embedding模型详解：OpenAI/Cohere/BGE三大方案对比
- 关于Embedding模型的那点事儿 - 知乎专栏


---
## 补充知识（来自网络搜索）
> query: embedding 模型对比 Cohere E5 text-embedding 使用场景 推荐 | 更新时间: 2026-04-19

### 嵌入模型对比与选型指南

*   **主流模型解析**
    *   **OpenAI:** 商业闭源 API，易用性强，生态完善，适合快速验证，但成本较高且针对英文优化明显。
    *   **Cohere:** 在检索任务上表现优异，支持多语言，上下文窗口大，适合企业级 RAG 应用。
    *   **E5 / BGE:** 顶级开源模型（如 BGE-M3, E5-large），支持本地部署，性价比高。BGE 和 E5 在 MTEB 榜单上表现强劲，尤其在中文和跨语言场景。

*   **核心对比维度**
    *   **性能:** 开源模型（BGE, E5）在多项基准测试中已逼近或超越 OpenAI Ada。
    *   **原理:** 均基于 Transformer 架构，差异在于训练数据规模与指令微调策略（如 E5 引入指令微调以提升区分度）。

*   **使用场景推荐**
    *   **RAG 检索:** 推荐 **Cohere v3** 或 **BGE-M3**，兼顾高召回率与多语言能力。
    *   **推荐系统:** 选用 **E5** 模型，因其在语义匹配与相似度计算任务上具有较好鲁棒性。

**来源：**
1. RAG 场景下的文本 Embedding 模型解析与选型指南
2. 最佳Embedding模型效果对比- OpenAI / Cohere / Google / E5 / BGE
3. 推荐场景下文本嵌入模型性能对比 - Gorse
4. Cohere vs OpenAI 嵌入模型深度评测


---
## 补充知识（来自网络搜索）
> query: BAAI 智源研究院 算法 模型 BGE 系列 | 更新时间: 2026-04-19

### BGE 系列模型技术概览

*   **定义与背景**
    *   BGE (BAAI General Embedding) 是由北京智源人工智能研究院（BAAI）发布的开源通用语义向量模型系列。
    *   该系列旨在提供高性能的文本嵌入与检索能力，支持中英双语及多语言场景。

*   **核心模型组件**
    *   **Embedding 模型**（如 BGE-M3）：支持多功能（稠密、稀疏、多向量）、多语言及长文本（8192 tokens）处理，用于生成高精度文本向量。
    *   **Re-Ranker 模型**（如 v2.0）：专门用于检索增强生成（RAG）阶段的精排，通过交叉注意力机制对检索结果进行二次打分，显著提升最终答案的相关性。

*   **技术特点与优势**
    *   **性能领先**：在 MTEB（海量文本嵌入基准）和 C-MTEB 等权威榜单上表现优异，部分指标超越同量级的商业模型。
    *   **开源生态**：模型权重与训练细节完全开源，支持本地化部署与微调。

*   **应用场景**
    *   RAG（检索增强生成）系统构建。
    *   语义搜索、知识库问答与文档检索。

**来源：**
1. BGE（BAAI General Embedding）模型详解 - CSDN博客
2. 智源（BAAI）更新BGE系列向量模型 - 知乎专栏
3. BGE向量算法_bge算法 - CSDN博客
4. RAG 再添新利器！智源开源最强检索排序模型 BGE Re-Ranker v2.0 - 智源社区


---
## 补充知识（来自网络搜索）
> query: BAAI 智源 模型 算法 BGE 其他模型 | 更新时间: 2026-04-19

### BGE（BAAI General Embedding）

*   **定义**：由北京智源人工智能研究院（BAAI）推出的开源通用嵌入模型系列，专注于生成高语义密度的向量表示。
*   **核心组件**：
    *   **Embedding 模型**：用于将文本转化为向量，进行初步检索。
    *   **Re-Ranker 模型**：如 BGE Re-Ranker v2.0，被称为目前开源的最强检索排序模型之一，用于对检索结果进行精细重排。
*   **应用与原理**：广泛应用于 RAG（检索增强生成）系统。通常采用“嵌入模型粗排 + Re-Ranker 精排”的架构，以平衡检索速度与精度。
*   **模型对比**：在构建检索系统时，常与阿里的 GTE 模型、Jina 模型等进行对比，需根据具体场景选择合适的搭配方案以提升检索效果。

**来源：**
1. 知乎专栏
2. 火山引擎开发者社区


---
## 补充知识（来自网络搜索）
> query: BGE-M3 dense retrieval sparse retrieval multi-vector retrieval use case | 更新时间: 2026-04-19

### BGE-M3 多功能检索模型

*   **核心定义**：BGE-M3 是一种支持三种检索模式的混合模型，旨在通过单一模型实现多语言、多粒度的检索能力。
*   **稠密检索**：生成单一高维向量表示文本语义，擅长处理同义词和语境理解，适用于广泛的语义匹配场景。
*   **稀疏检索**：基于词频和重要性生成稀疏向量（类似于 SPLADE），擅长处理精确关键词匹配和领域专有名词，弥补语义模糊的不足。
*   **多向量检索**：基于 ColBERT 机制，将文本编码为令牌级向量并计算交互得分，兼顾语义相关性与字面精确匹配。
*   **应用场景**：常与向量数据库（如 Qdrant）结合实现混合搜索，通过加权融合三种模式的结果，提升复杂查询的召回准确率。

**来源：**
- bge-m3 Model by BAAI - nvidia nim
- Solved: Re:Bge-m3 model... - Intel Community
- My Journey into Hybrid Search... - r/vectordatabase
